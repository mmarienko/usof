import React from 'react';

import {Container, Typography} from '@material-ui/core';

const Profile = () => {
   return (
      <Container>
         <Typography>
            Profile
         </Typography>
      </Container>
   )
}

export default Profile;