import React from 'react';

import {Container, Typography} from '@material-ui/core';

const Sponsor = () => {
   return (
      <Container>
         <Typography>
            Sponsor
         </Typography>
      </Container>
   )
}

export default Sponsor;